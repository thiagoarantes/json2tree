import { styled } from "@stitches/react";

const OuterContainer = styled("div", {
  height: "100%",
  width: "100%",
  backgroundColor: "grey",
});

function App() {
  return <OuterContainer>Future json2Tree App (WIP)</OuterContainer>;
}

export default App;
